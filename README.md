# CryptoFlow-Pipeline
A modular crypto data pipeline that extracts API data, persists raw records, validates and cleans incoming datasets, transforms them into analytics-ready models with dbt, and orchestrates the full workflow with Airflow. The platform can be extended with MLflow to support experiment tracking and simple forecasting or anomaly detection use cases.
